# OpenCode Harness

## Skill reference

Load `openrouter-agent` (`skills/openrouter-agent/SKILL.md`).

## Setup

1. CLI: `uv sync` + `.env`.
2. MCP: add to `~/.config/opencode/opencode.json` (reference `tools/MCP/opencode.md`). OAuth runs automatically.
3. [PERSON_NAME]: use this harness guide.

Reference: `SKILL.md`, `tools/MCP/opencode.md` (URL: `https://mcp.openrouter.ai/mcp`), `benchmarks.py`, docs (`[PERSON_NAME] [PERSON_NAME]`), lines 38–50.
