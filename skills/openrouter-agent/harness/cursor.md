# Cursor Harness

## Skill reference

Load `openrouter-agent` (`skills/openrouter-agent/SKILL.md`).

## Setup

1. CLI: `uv sync` + `.env`.
2. MCP: add to `~/.cursor/mcp.json` (reference `tools/MCP/cursor.md`), verify with `cursor-agent mcp list`, auth via settings or first tool use.
3. [PERSON_NAME]: use this harness guide.

Reference: `SKILL.md`, `tools/MCP/cursor.md` (URL: `https://mcp.openrouter.ai/mcp`), `benchmarks.py`, docs (`[PERSON_NAME] [PERSON_NAME]`), lines 53–63.
