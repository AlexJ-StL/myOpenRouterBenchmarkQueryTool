# Codex Harness

## Skill reference

Load `openrouter-agent` (`skills/openrouter-agent/SKILL.md`). Reference CLI (`benchmarks.py`) and MCP server (`https://mcp.openrouter.ai/mcp`).

## Setup

1. CLI: `uv sync` + `.env`.
2. MCP: follow `tools/MCP/codex.md` (`codex mcp add openrouter --url https://mcp.openrouter.ai/mcp`, `codex mcp login openrouter`).
3. [PERSON_NAME]: use `skills/openrouter-agent/harness/codex.md`.

Reference: `SKILL.md`, `tools/MCP/codex.md`, docs (`[PERSON_NAME] [PERSON_NAME]`), lines 31–34.
